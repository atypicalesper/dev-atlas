import SpecialClient from './special-client';

export const metadata = {
  title: 'DSA Sprint',
  description: 'A focused roadmap for algorithmic fluency, speed, and interview pattern recognition.',
};

export default function SpecialPage() {
  return <SpecialClient />;
}
